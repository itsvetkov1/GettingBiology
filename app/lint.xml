<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Dependency/SDK update warnings are intentionally ignored for phase-1 rework: task forbids SDK, Gradle, plugin, and dependency bumps. -->
    <issue id="AndroidGradlePluginVersion" severity="ignore" />
    <issue id="GradleDependency" severity="ignore" />
    <issue id="NewerVersionAvailable" severity="ignore" />
    <issue id="OldTargetApi" severity="ignore" />

    <!-- Legacy/generated resource cleanup is outside this phase; shrinking remains enabled and SF-6 verifies APK size. -->
    <issue id="UnusedResources" severity="ignore" />
    <issue id="ObsoleteSdkInt" severity="ignore" />
    <issue id="MonochromeLauncherIcon" severity="ignore" />
    <issue id="IconLocation" severity="ignore" />

    <!-- Layout micro-optimizations are not user-visible phase-1 blockers. -->
    <issue id="Overdraw" severity="ignore" />
    <issue id="DisableBaselineAlignment" severity="ignore" />
    <issue id="MergeRootFrame" severity="ignore" />

    <!-- Existing quantity strings are short dashboard labels; converting them to plurals is deferred to localization cleanup. -->
    <issue id="PluralsCandidate" severity="ignore" />

    <!-- KTX style rewrites are non-functional churn; leave them for a separate style pass. -->
    <issue id="UseKtx" severity="ignore" />
</lint>
